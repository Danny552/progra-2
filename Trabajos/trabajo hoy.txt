punto 1:
#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int absol = abs(-5);
    int eleva = pow(20 , 2) + pow(-5 , 2);
    int raizyvalor = sqrt(100) + abs(-5);
    int pisoytecho = floorf(7.5) - ceilf(-0.5);
    int potmasraiz = pow(4 , 5) + pow(15, 1/3);
} 
punto 2:
#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    float rma, rme, h;
    float f;
    float x2, x1, y2, y1;
    float pi = 3.14159265358;
    float volumen_de_un_cono_truma = pi * (pow(rma , 2) + pow(rme , 2) + rma * rme) * (h/3);
    float fahrenheit_a_celsius = (f - 32) * 5/9;
    float distancia_entre_dos_puntos = sqrt(pow((x2 - x1) , 2) + pow((y2 - y1) , 2));
    float pendiente_de_una_recta = (y2 - y1)/(y2 - y1);
}
punto 3:
#include <stdio.h>
#include <math.h>
#include <stdlib.h>
int main()
{
    int num1 = 345;
    int num2 = 456;
    int inver1 = ((num1 % 10) * 100) + ((num1 % 100) - (num1 % 10)) + (num1 / 100);
    int inver2 = (((num2 % 100) - (num2 % 10))* 10) + ((num2 / 100) * 10) + (num2 % 10);
    printf ("%d\n", inver1);
    printf ("%d\n", inver2);
    int suma = (((inver1 / 100) + (inver2 /100)) * 100) + (((inver1 % 100) - (inver1 % 10)) + ((inver2 % 100) - (inver2 % 10))) + ((inver1 % 10) + (inver2 % 10));
    printf ("%d\n", suma);
} 